import { analysers } from "superdough";

// We'll use a simple global bus or just export references
// Since the original code relies on global elements and superdough's exported analysers,
// we can continue using that pattern but in a more structured way if needed.
// For now, let's just make sure analysers are accessible.

export { analysers };
